const escapeHtml = (value = '') => String(value).replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[char]));

const money = (value) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR' }).format(Number(value || 0));

function rowClass(value) {
  return value >= 0 ? 'income' : 'expense';
}

function buildReportHtml(data, compact = false) {
  const metrics = (data.metrics || []).map((item) => `<div class="metric"><span>${escapeHtml(item.label)}</span><strong>${escapeHtml(item.value)}</strong></div>`).join('');
  const modes = Object.entries(data.modes || {}).map(([key, value]) => `<span>${escapeHtml(key.toUpperCase())}: <b>${money(value)}</b></span>`).join(' · ');

  const participantCards = (data.rows || []).map((row) => {
    const paidText = row.personalSpend > 0 ? `Paid ${money(row.directPaid)} + spend ${money(row.personalSpend)}` : `Paid ${money(row.paid)}`;
    const balanceText = row.balance >= 0 ? `Receives ${money(row.balance)}` : `Pays ${money(Math.abs(row.balance))}`;
    const balanceClass = row.balance >= 0 ? 'receive' : 'pay';
    return `<div class="split-row"><div><strong>${escapeHtml(row.name)}</strong><span>${escapeHtml(`${row.quantity} qty · ${paidText} · share ${money(row.fairShare)}`)}</span></div><b class="${balanceClass}">${escapeHtml(balanceText)}</b></div>`;
  }).join('');

  const participantTable = (data.rows || []).length
    ? `<table class="table"><thead><tr><th>Name</th><th>Qty</th><th>Paid</th><th>Personal</th><th>Share</th><th>Balance</th></tr></thead><tbody>${(data.rows || []).map((row) => `<tr><td>${escapeHtml(row.name)}</td><td>${escapeHtml(String(row.quantity))}</td><td>${money(row.paid)}</td><td>${money(row.personalSpend)}</td><td>${money(row.fairShare)}</td><td class="${rowClass(row.balance)}">${row.balance >= 0 ? `Receives ${money(row.balance)}` : `Pays ${money(Math.abs(row.balance))}`}</td></tr>`).join('')}</tbody></table>`
    : `<p>${escapeHtml(data.text?.noData || 'No records')}</p>`;

  const selectedRows = (data.selectedSources || []).map((row) => `<tr><td>${escapeHtml(row.date)}</td><td>${escapeHtml(row.kind)}</td><td>${escapeHtml(row.type)}</td><td class="${rowClass(row.type === 'expense' ? -1 : 1)}">${money(row.amount)}</td><td>${escapeHtml(row.mode)}</td><td>${escapeHtml(row.note)}</td></tr>`).join('');
  const settlementRows = (data.settlements || []).map((row) => `<tr><td>${escapeHtml(row.from)}</td><td>${escapeHtml(row.to)}</td><td>${money(row.amount)}</td></tr>`).join('');
  const fundRows = (data.fundAllocations || []).map((row) => `<tr><td>${escapeHtml(row.from)}</td><td>${escapeHtml(row.to)}</td><td>${money(row.amount)}</td></tr>`).join('');
  const reimbursementRows = (data.reimbursements || []).map((row) => `<tr><td>${escapeHtml(row.name)}</td><td>${escapeHtml(row.reason)}</td><td>${money(row.amount)}</td></tr>`).join('');
  const transactionCards = (data.rows || []).slice(0, 8).map((row) => `<div class="split-row"><div><strong>${escapeHtml(row.date)}</strong><span>${escapeHtml(`${row.type} · ${row.mode}`)}</span></div><b class="${row.type === 'expense' ? 'pay' : 'receive'}">${money(row.amount)}</b></div>`).join('');

  const splitSections = compact
    ? `<section class="split-list">${participantCards || `<p>${escapeHtml(data.text?.noData || 'No records')}</p>`}</section>`
    : `<section class="section"><h2>Participant breakdown</h2>${participantTable}</section>${(data.selectedSources || []).length ? `<section class="section"><h2>Selected records</h2><table class="table"><thead><tr><th>Date</th><th>Kind</th><th>Type</th><th>Amount</th><th>Mode</th><th>Note</th></tr></thead><tbody>${selectedRows}</tbody></table></section>` : ''}${settlementRows ? `<section class="section"><h2>Settlements</h2><table class="table"><thead><tr><th>From</th><th>To</th><th>Amount</th></tr></thead><tbody>${settlementRows}</tbody></table></section>` : ''}${fundRows ? `<section class="section"><h2>Fund allocations</h2><table class="table"><thead><tr><th>From</th><th>To</th><th>Amount</th></tr></thead><tbody>${fundRows}</tbody></table></section>` : ''}${reimbursementRows ? `<section class="section"><h2>Reimbursements</h2><table class="table"><thead><tr><th>Name</th><th>Reason</th><th>Amount</th></tr></thead><tbody>${reimbursementRows}</tbody></table></section>` : ''}`;

  const transactionSection = compact
    ? (data.rows || []).length ? `<section class="section"><h2>${escapeHtml(data.text?.details || 'Details')}</h2><div class="split-list">${transactionCards}</div></section>` : ''
    : `<section class="section"><h2>${escapeHtml(data.text?.details || 'Details')}</h2>${(data.rows || []).length ? `<table class="table"><thead><tr><th>Date</th><th>Type</th><th>Amount</th><th>${escapeHtml(data.text?.mode || 'Mode')}</th><th>Note</th></tr></thead><tbody>${(data.rows || []).map((row) => `<tr><td>${escapeHtml(row.date)}</td><td>${escapeHtml(row.type)}</td><td class="${row.type === 'expense' ? 'expense' : 'income'}">${money(row.amount)}</td><td>${escapeHtml(row.mode)}</td><td>${escapeHtml(row.note)}</td></tr>`).join('')}</tbody></table>` : `<p>${escapeHtml(data.text?.noData || 'No records')}</p>`}</section>`;

  return `<!doctype html><html><head><meta charset="utf-8"><style>
    *{box-sizing:border-box} body{margin:0;font-family:Arial,sans-serif;color:#14213d;background:#eef3fb;padding:${compact ? '16px' : '32px'}} .report{max-width:${compact ? '760px' : '980px'};margin:auto;background:#fff;border-radius:${compact ? '20px' : '24px'};overflow:hidden;box-shadow:0 16px 36px rgba(15,23,42,.12)} .head{padding:${compact ? '20px 22px' : '30px 32px'};background:#0f172a;color:#fff} .brand{font-size:12px;font-weight:800;letter-spacing:1.25px;opacity:.82;text-transform:uppercase} .head h1{margin:10px 0 6px;font-size:${compact ? '24px' : '30px'};line-height:1.15} .head p{margin:0;opacity:.86;font-size:${compact ? '13px' : '14px'}} .metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(${compact ? '128px' : '145px'},1fr));gap:${compact ? '10px' : '12px'};padding:${compact ? '16px' : '24px'}} .metric{border:1px solid #e2e8f0;border-radius:16px;padding:${compact ? '12px' : '16px'};background:#f8fbff} .metric span{display:block;color:#64748b;font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.08em} .metric strong{display:block;margin-top:8px;font-size:${compact ? '17px' : '20px'};color:#0f172a} .section{padding:0 ${compact ? '18px' : '24px'} ${compact ? '18px' : '24px'}} .section h2{font-size:${compact ? '15px' : '16px'};margin:0 0 12px;color:#0f172a} .modes{padding:14px 16px;background:#eff6ff;border-radius:12px;color:#334155;font-size:13px;border:1px solid #dbeafe} .table{width:100%;border-collapse:collapse;font-size:${compact ? '11px' : '12px'};border:1px solid #e2e8f0;border-radius:12px;overflow:hidden} .table th{background:#f8fafc;text-align:left;padding:10px;color:#475569;font-size:11px;text-transform:uppercase;letter-spacing:.08em} .table td{padding:10px;border-top:1px solid #edf2f7;color:#0f172a;vertical-align:top} .income{color:#059669;font-weight:700} .expense{color:#e11d48;font-weight:700} .split-list{padding:0 ${compact ? '18px' : '24px'} ${compact ? '18px' : '24px'};display:grid;gap:${compact ? '8px' : '10px'}} .split-row{display:flex;align-items:center;justify-content:space-between;gap:${compact ? '12px' : '18px'};background:#172033;color:#fff;border-radius:${compact ? '12px' : '14px'};padding:${compact ? '12px 14px' : '15px 18px'}} .split-row strong{display:block;font-size:${compact ? '15px' : '18px'}} .split-row span{display:block;margin-top:4px;color:#a8b3c7;font-size:${compact ? '11px' : '12px'}} .split-row b{font-size:${compact ? '15px' : '18px'};white-space:nowrap} .receive{color:#34d399} .pay{color:#fb7185} .footer{padding:18px 24px;color:#64748b;font-size:11px;border-top:1px solid #e2e8f0;background:#fff}</style></head><body><main class="report ${compact ? 'compact' : ''}"><header class="head"><div class="brand">${escapeHtml(data.user?.shopName || data.user?.name || 'DESKLITE')}</div><h1>${escapeHtml(data.title || 'Report')}</h1><p>${escapeHtml(data.subtitle || '')}</p></header><section class="metrics">${metrics}</section>${modes ? `<section class="section"><div class="modes">${modes}</div></section>` : ''}${data.kind === 'split' ? splitSections : transactionSection}<footer class="footer">${escapeHtml(data.text?.generated || 'Generated')} · ${new Date().toLocaleString('en-IN')}${data.truncated ? ' · Detail rows truncated' : ''}</footer></main></body></html>`;
}

module.exports = { buildReportHtml };
